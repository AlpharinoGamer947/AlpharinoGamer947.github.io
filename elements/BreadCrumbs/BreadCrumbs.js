let breadcrumbsArray = [];

breadcrumbsArray.push({
  name: "Home", link: "html/index.html"
})

breadcrumbsArray.forEach((htmlpage) => {
  console.log(htmlpage);
})

function addPage(params) {
  breadcrumbsArray.push
}